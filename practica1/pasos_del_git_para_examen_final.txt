como proteger el main

ir a ajustes 
ir a branch
dar click en add classic branch protection rule
escribir main en branch name pattern
dar click en el primer inciso 
dar click en create 

pasos para subir por ramas 

git branch (nombre de la rama) 
git checkout (nombre de la rama)
git add .
git commit -m (nombre de como aparezca en el repositorio)
git checkout main (pasarse al main)
git merge
git push origin (nombre de la rama) 
git pull origin (nombre de la rama) 
git merge 
git push origin main 

para archivos txt

git commit -f (nombre de la rama) solo si al momento de subir con git add
. no aparece en el git hub 

 